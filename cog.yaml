build:
  gpu: true
  python_version: "3.12"
  run:
    - "curl -LsSf https://astral.sh/uv/install.sh | sh"
    - "uv pip install demucs-inference --torch-backend=auto"
    - "demucs models download --all"
predict: "cog.py:Predictor"